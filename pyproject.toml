[project]
name = "call-me-maybe"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "numpy==2.2.6",
    "pydantic>=2.11.0",
	"torch",
    "transformers>=4.40.0",
    "huggingface-hub>=0.20.0",
    "accelerate",
    "fire>=0.7.1",
    "colorama>=0.4.6",
]

[dependency-groups]
dev = [
    "flake8",
    "mypy",
]